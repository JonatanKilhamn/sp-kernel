function shortestPathMatrix = findShortestPaths(Graph)
% computes all shortest

% take a single graph
% return a distance matrice shortestPaths

% this function might not be used, if preprocessing results are used in the end

shortestPathMatrix = floydwarshall_mod(Graph.am);

end
